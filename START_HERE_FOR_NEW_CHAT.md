# START HERE

Если новый агент входит в `GOSHA_MOBILE`, начинай отсюда.

## Сначала прочитать

1. `AGENTS.md`
2. `/home/max/GOSHA_PLATFORM/docs/GOSHA_PROJECT_MAP_RU.md`
3. `README_RU.md`
4. `docs/NEW_CHAT_CHECKPOINT_RU.md`
5. `docs/AGENT_CHECKPOINT_RU.md`
6. `docs/PROJECT_STATUS_RU.md`
7. Если работаешь с Android-кодом:
   - `app/AGENTS.md`
8. Если работаешь с iOS-кодом:
   - `ios/AGENTS.md`
   - `docs/GOSHA_MOBILE_IOS_HANDOFF_RU.md`
   - при iOS-first задаче предпочитай отдельный репозиторий:
     - `/Users/maksim/Developer/GOSHA_MOBILE_IOS`
     - `https://github.com/MaxCorpOrg/GOSHA_MOBILE_IOS`

## Что это за проект

- Это отдельный Android-клиент платформы `Гоша`.
- Теперь это Android-first репозиторий.
- Он больше не должен развиваться как часть `AI_ROBOT`.
- Для iOS уже выделен отдельный репозиторий:
  - `https://github.com/MaxCorpOrg/GOSHA_MOBILE_IOS`
- Старый клиент из `AI_ROBOT` остаётся легаси-контуром для сравнения и миграции.

## Что важно помнить

- Пользовательский бренд: `Гоша`
- Новый рабочий контур по умолчанию:
  - панель `18876`
  - серверный контур `18080`
- Клиент должен принимать оба префикса Wi‑Fi робота:
  - `GOSHA-`
  - `Xiaozhi-`
- В текстах и интерфейсе пользователю показывать только `GOSHA-`
- Если нужно быстро свериться, где серверный голос, панель и прошивка, смотри:
  - `/home/max/GOSHA_PLATFORM/docs/GOSHA_PROJECT_MAP_RU.md`

## Обязательный старт нового чата

Перед тем как предлагать работу, агент обязан:

1. Прочитать этот файл и актуальные checkpoint-документы.
2. Выполнить `git status --short --branch`.
3. Выполнить `git log --oneline -1`.
4. Если задача затрагивает общий контур `Гоша`, свериться со свежим входным файлом на рабочем столе.
5. В первом содержательном ответе явно сообщить:
   - текущую ветку;
   - текущую стадию;
   - следующий приоритетный шаг.

## Актуальная рабочая точка

- Для локального портала не начинать из `main`, а использовать отдельное рабочее дерево:
  - `/home/max/worktrees/gosha/mobile-portal`
- Текущая рабочая ветка этого контура:
  - `feature/mobile-hotspot-portal`
- Ветка создана для устранения пустого экрана и разрыва сети в `HotspotPortalActivity`.
- Текущая стадия:
  - встроенный локальный портал снова работает именно внутри приложения и подтверждён на живом телефоне;
  - поверх этого теперь реализована отдельная стабилизация после успешного подключения:
    - введён флаг `setupCompleted`;
    - после успешного возврата в `MENU` приложение больше не должно само выбрасывать пользователя назад в шаг переподключения;
    - добавлен тихий фоновый контур проверки сети, панели и локального адреса без смены экрана;
  - мобильный клиент теперь сам сообщает в панель `mobile_presence`:
    - `phone_on_robot_wifi`
    - `robot_hotspot_visible`
    - `home_wifi_local`
    - `not_found`
  - `2026-05-27` поверх этого ускорен и сделан понятнее сам путь проверки состояния:
    - `MENU` теперь сразу показывает, что именно происходит:
      - проверка связи;
      - телефон не в домашнем `Wi‑Fi`;
      - рядом видна сеть робота;
      - телефон уже в сети робота;
    - шаг повторного подключения `Wi‑Fi` теперь сам уточняет текущее состояние робота, не дожидаясь ручного запуска;
    - локальный поиск сначала проверяет сохранённый `robotHost`, а потом идёт в общий поиск по подсети;
    - сокращены паузы после `submit` и после возврата из локального портала;
  - `2026-06-30` живой прогон уточнил промежуточную границу проблемы:
    - робот в домашней сети был доступен по адресу `192.168.1.159:8080` с рабочего компьютера;
    - панель тоже хранила `board_ip = 192.168.1.159`;
    - на телефоне одновременно был активен VPN `run.nlab.iproxy` / `org.amnezia.vpn`, из-за чего локальный канал до робота не проходил;
    - мобильный клиент для этого уже был доработан так, чтобы:
      - использовать `Wi‑Fi`-привязанный `HTTP`-клиент для локальных проверок;
      - подхватывать `board_ip` из runtime панели как подсказку для быстрого поиска;
      - честно показывать в `MENU` и на шаге повторного `Wi‑Fi`, что локальную проверку сейчас блокирует VPN, а не сам робот;
  - `2026-07-01` живой прогон на `TECNO LI9` продвинул Android-контур дальше:
    - найден ещё один реальный мобильный фактор:
      - пока на телефоне выключена системная геолокация, приложение не видит свежие результаты `Wi‑Fi`-сканирования и слишком долго держит устаревшие состояния;
    - после включения геолокации подтверждено:
      - `MENU` честно видит рядом сеть робота `GOSHA-A-1BE1`;
      - системный Android-диалог на временное подключение к сети робота открывается штатно;
      - после выбора `GOSHA-A-1BE1` встроенный `HotspotPortalActivity` снова открывает страницу робота именно внутри приложения;
    - подтвержден полный живой цикл локального портала:
      - страница робота доходит до `done.html`;
      - в живом журнале зафиксировано `Exit config mode request sent`;
      - после этого робот выходит из своей точки доступа;
      - после возврата телефона в домашний `Wi‑Fi` приложение находит робота локально по адресу `192.168.0.102`;
      - `MENU` показывает честное состояние `Робот в сети и готов к работе`;
    - поверх этого уже внесены отдельные UX-правки:
      - убран ложный статичный верхний статус `Робот на связи`;
      - верхний блок `MENU` теперь зависит от живого состояния;
      - при выключенном `Wi‑Fi` или устаревшем `SSID` приложение больше не притворяется, что просто не нашло робота в домашней сети;
      - если робот уже принял настройки и вышел из режима подключения, но телефон ещё не вернулся в домашний `Wi‑Fi`, `MENU` теперь прямо просит вернуть телефон в домашнюю сеть, а не показывает расплывчатый статус про уточнение адреса;
    - текущий главный незакрытый хвост теперь уже другой:
      - пока процесс привязан к сети робота `192.168.4.x`, обращения клиента в панель по `mobile_presence` и runtime могут уходить в таймаут, потому что у точки доступа робота нет внешнего интернета;
      - следующий шаг — развести локальный трафик робота и внешний трафик панели, а затем подтвердить стабильный сигнал в панели администратора во время и сразу после подключения;
  - `2026-07-02` поверх этого уже внесён прикладной сетевой фикс:
    - `RobotWifiConnector` больше не переводит весь процесс приложения на сеть робота через `bindProcessToNetwork(...)`;
    - сеть робота по-прежнему удерживается и используется для адресных локальных запросов к порталу и `ws://<robot>:8080`;
    - запросы в панель и другой внешний контур должны снова идти по обычному интернет-маршруту телефона, а не застревать в `192.168.4.x`;
  - `2026-07-02` сразу после этого подтверждён и следующий живой результат:
    - на `TECNO LI9` при домашнем `Wi‑Fi` `AX3000T-4039` и одновременно активном VPN `run.nlab.iproxy` приложение больше не сдаётся заранее;
    - `MainActivity` теперь не прерывает локальный поиск только из-за VPN, потому что поиск уже идёт через `Wi‑Fi`-привязанный клиент;
    - приложение больше не отправляет в панель ложный `not_found`, если реальная проблема только в сетевой политике телефона;
    - живой журнал подтвердил:
      - `discoverRobotLocally(subnet=192.168.1, preferred=[192.168.1.159])`;
      - успешное нахождение `192.168.1.159`;
      - успешную отправку `mobile_presence = home_wifi_local`;
    - `shared_prefs` на телефоне обновились до:
      - `robot_host = 192.168.1.159`;
      - `connector_robot_host = 192.168.1.159`;
    - runtime панели после этого уже показывает:
      - `mobile_presence.state = home_wifi_local`;
      - `mobile_presence.local_host = 192.168.1.159`;
      - `connectivity.connected = true`;
      - `connectivity.evidence = local_host`;
    - отдельная выдержка `MENU` дольше 60 секунд пройдена:
      - экран остался на честном состоянии `Робот в сети`;
      - самопроизвольного отката назад не произошло;
  - `2026-07-02` следующим живым прогоном закрыт ещё один скрытый Android-дефект локального поиска:
    - робот и панель уже были исправны, но `LocalRobotDiscovery` делал ручной проверочный обмен запрос-ответ по `ws://<host>:8080/ws` в главном потоке;
    - из-за `NetworkOnMainThreadException` приложение затягивало локальную проверку и могло ложно срываться в отрицательный результат, хотя робот был доступен;
    - проверочный обмен вынесен в рабочий поток;
    - в локальный поиск добавлены:
      - приоритетная быстрая проверка сохранённого `robotHost`;
      - быстрый TCP-предфильтр `:8080`;
      - отдельный ручной проверочный обмен по `ws://<host>:8080/ws` уже вне главного потока;
    - на живом телефоне после этого подтверждено:
      - `MENU` снова быстро выходит в честное состояние `Робот в сети`;
      - локальный адрес `192.168.1.159` снова находится и сохраняется;
      - `ConnectorForegroundService` живёт дольше 3 минут после сворачивания приложения;
      - runtime панели остаётся свежим:
        - `mobile_presence.state = home_wifi_local`;
        - `mobile_presence.local_host = 192.168.1.159`;
        - `connectivity.connected = true`;
  - `ConnectorForegroundService` зарегистрирован в `AndroidManifest`, оформлен под `targetSdk 34` и должен автоматически запускаться, когда у приложения есть реальный `robotHost`;
  - локальные проверки уже подтверждены:
    - `./gradlew --no-daemon testClientDebugUnitTest`
    - `./gradlew --no-daemon assembleClientDebug`
    - `./gradlew --no-daemon lintClientDebug`
  - на телефоне оставлен только один актуальный пакет:
    - `com.maxcorp.gosha.mobile`
  - подтверждено, что `HotspotPortalActivity` удерживает телефон в `GOSHA-A-*`;
  - подтверждено, что встроенный `WebView` получает `200 OK` от `http://192.168.4.1`, загружает HTML и выполняет запросы `/saved/list`, `/scan`, `/advanced/config`;
  - `2026-07-12` правка восстановления портала подтверждена живым циклом на `TECNO LI9` и `gosha-main`:
    - после ручного выключения и включения `Wi‑Fi` телефона приложение повторно запросило `GOSHA-A-1BE1` и восстановило портал без перезапуска;
    - новый маршрут `candidate=600` получил `200`, а `candidate=default` для `192.168.4.1` не использовался;
    - `POST /submit` получил `200` примерно через `3,3` секунды, затем `done.html` и `/exit` получили `200`;
    - телефон вернулся в `AX3000T-4039`, робот получил `192.168.1.159`, приложение осталось в `MENU` без отката назад;
    - панель получила `mobile_presence = home_wifi_local` и `local_host = 192.168.1.159`;
    - после чистой фоновой выдержки больше `218` секунд PID `19519` и `ConnectorForegroundService` сохранились, реальные проверки продолжили давать `executed ok=true`;
    - установленный APK: SHA-256 `2d107ddf596bbbaf1e6eaff680078fa3ef41552bd29cf5bb1f9d4ff9650683d1`;
    - коммит кода в рабочей ветке: `86fd525 Восстановить портал после потери Wi-Fi`;
  - важное текущее состояние git:
    - последняя серия этой точки затрагивает:
      - `app/src/main/java/com/maxcorp/edgeconnector/MainActivity.kt`
      - `app/src/main/java/com/maxcorp/edgeconnector/LocalRobotDiscovery.kt`
      - `app/src/main/java/com/maxcorp/edgeconnector/LocalPortProbe.kt`
      - `app/src/main/java/com/maxcorp/edgeconnector/LocalWsHandshakeProbe.kt`
      - `app/src/main/java/com/maxcorp/edgeconnector/RobotWifiConnector.kt`
      - `app/src/main/java/com/maxcorp/edgeconnector/WifiInfoHelper.kt`
      - `app/src/main/res/layout/activity_main.xml`
      - `app/src/main/res/values/strings.xml`
    - перед новым коммитом и новым чатом всё равно обязательно проверить `git status --short --branch`.
- Следующие Android-приоритеты:
  - P2: ограничить повторные системные запросы сети, пока `Wi‑Fi` телефона полностью выключен; сейчас `/scan` повторяет попытку примерно раз в пять секунд;
  - P1: исправить подтверждение OEM-режима `Нет ограничений`, чтобы `Позже` или простое открытие настроек не скрывало инструкцию навсегда;
  - P2: закрепить повторяемый регрессионный сценарий Android + прошивка + платформа и контрольные суммы выпуска;
  - отдельно позже проверить переходный радиопрефикс `Xiaozhi-*`;
  - не использовать внешние raw TCP-проверки и `nc -z` на порту робота `:8080`.

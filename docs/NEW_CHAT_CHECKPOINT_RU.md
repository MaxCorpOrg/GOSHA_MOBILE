# NEW CHAT CHECKPOINT

Короткая точка входа для следующего агента в `GOSHA_MOBILE`.

## Сначала прочитать

1. `../AGENTS.md`
2. `/home/max/GOSHA_PLATFORM/docs/GOSHA_PROJECT_MAP_RU.md`
3. `NEW_CHAT_CHECKPOINT_RU.md`
4. `AGENT_CHECKPOINT_RU.md`
5. `PROJECT_STATUS_RU.md`
6. Если работаешь с Android-кодом:
   - `../app/AGENTS.md`
7. Если работаешь с iOS-кодом:
   - `../ios/AGENTS.md`
   - `GOSHA_MOBILE_IOS_HANDOFF_RU.md`

## Что уже сделано

- Создан отдельный Android-проект `GOSHA_MOBILE`.
- Для проекта уже настроен `origin`:
  - `git@github.com:MaxCorpOrg/GOSHA_MOBILE.git`
- Ветка `main` уже отправлена в GitHub и отслеживает:
  - `origin/main`
- Публичный URL проекта:
  - `https://github.com/MaxCorpOrg/GOSHA_MOBILE`
- Новый клиент переводится на бренд `Гоша`.
- Старый контур `AI_ROBOT` больше не является рабочим корнем этого проекта.
- В клиент добавляется чтение `mobile_profile` из `GOSHA_PLATFORM`.
- В переходный период клиент принимает `GOSHA-` и `Xiaozhi-`.
- Проект уже подтверждён локальной сборкой:
  - `assembleClientDebug`
  - `testClientDebugUnitTest`
- В репозиторий уже добавлен отдельный iOS-каркас:
  - `ios/GoshaMobileIOS`
- Для iOS уже создан отдельный GitHub-репозиторий:
  - `https://github.com/MaxCorpOrg/GOSHA_MOBILE_IOS`
- Локальный отдельный рабочий корень iOS:
  - `/Users/maksim/Developer/GOSHA_MOBILE_IOS`
- iOS-каркас уже переведён на новый контракт `GOSHA_PLATFORM`:
  - `bundle.mobile_profile`
  - runtime `connectivity`
  - `GOSHA-` + переходный `Xiaozhi-`
- Для iOS уже локально собран `XcodeGen 2.30.0`:
  - `/Users/maksim/bin/xcodegen`
- Через него уже создан:
  - `ios/GoshaMobileIOS/GoshaMobileIOS.xcodeproj`
- Локальная Swift-проверка уже подтверждена:
  - `xcrun swiftc -typecheck ...`
- И уже подтверждена сборка через:
  - `xcodebuild ... CODE_SIGNING_ALLOWED=NO build`
- В iOS `Info.plist` уже добавлены:
  - явные `panel/legal/portal` URL;
  - `GOSHA-` + `Xiaozhi-` SSID prefixes;
  - `ATS`-исключения для `151.241.228.232:18876` и `192.168.4.1`;
  - `NSLocalNetworkUsageDescription`
- И уже подтверждён запуск в iOS Simulator:
  - `xcrun simctl install booted .../Гоша.app`
  - `xcrun simctl launch booted com.maxcorp.gosha.mobile.ios`
- Для репозиториев `GOSHA_PLATFORM` и `GOSHA_MOBILE` уже поднят локальный фоновой sync:
  - скрипт `/Users/maksim/bin/gosha_repo_sync.sh`
  - `launchd` job `com.maxcorp.gosha-repo-sync`
  - отчёт `/Users/maksim/Developer/.gosha-sync/last_report.txt`
- Общая карта связанных контуров теперь зафиксирована в:
  - `/home/max/GOSHA_PLATFORM/docs/GOSHA_PROJECT_MAP_RU.md`
- Исправлена честность runtime-снимка:
  - Android больше не считает робота "подключённым через панель" только из-за настроенного endpoint
  - новый источник истины — блок `connectivity` из `GOSHA_PLATFORM`
- Исправлена и следующая нечестность:
  - если панель уже подтвердила робота, но `local_host` ещё не пришёл, приложение больше не называет это "робот найден в вашей сети"
  - теперь это отдельное честное состояние:
    - робот уже на связи с платформой;
    - локальный адрес ещё уточняется
- Каноническая Android-проверка после этой точки:
  - `./gradlew --no-daemon clean assembleClientDebug testClientDebugUnitTest lintClientDebug`
  - запускать последовательно одним вызовом, а не несколькими параллельными задачами

## Где остановились

- В `GOSHA_MOBILE` уже унифицированы правила общения агента:
  - корневой и локальные `AGENTS.md` требуют понятный русский технический язык;
  - для типовых терминов закреплены русские формы и пояснения.
- Для локального портала поднято отдельное рабочее дерево:
  - `/home/max/worktrees/gosha/mobile-portal`
- Текущая рабочая ветка этого контура:
  - `feature/mobile-hotspot-portal`
- В этой ветке уже сделана рабочая правка пользовательского пути локального портала:
  - `MainActivity.startRobotWifiPortal()` снова открывает встроенный экран `HotspotPortalActivity`, а не внешний браузер;
  - сам экран загружает портал робота через `RobotPortalClient` и использует найденный `Network` робота;
  - дополнительные диагностические сообщения `HotspotPortal` и `GoshaRobotWifi` оставлены на месте.
- Тем же днём поверх рабочего портала добавлена новая стабилизация Android-контура:
  - в локальном состоянии появился флаг `setupCompleted`;
  - после успешного возврата в `MENU` приложение больше не должно само открывать шаг переподключения только потому, что рядом ещё видна сеть робота или панель ответила с задержкой;
  - добавлен тихий фоновый контур проверки `Wi‑Fi` / панели / локального адреса без смены экрана;
  - мобильный клиент теперь сам отправляет в панель `mobile_presence` со состояниями `phone_on_robot_wifi`, `robot_hotspot_visible`, `home_wifi_local`, `not_found`;
  - `ConnectorForegroundService` теперь зарегистрирован в `AndroidManifest`, оформлен под Android 14 и должен стартовать автоматически при наличии реального `robotHost`.
- Поверх этой стабилизации следующая локальная серия `2026-05-27` уже добивает скорость и прозрачность:
  - `MENU` теперь должен сразу показывать, что приложение проверяет робота, не видит домашний `Wi‑Fi`, видит рядом сеть робота или уже нашло локальный адрес;
  - шаг повторного подключения `Wi‑Fi` теперь сам уточняет текущее состояние робота, не дожидаясь ручного запуска;
  - локальный поиск сначала проверяет сохранённый `robotHost`;
  - сокращены паузы после `submit` и после возврата из локального портала.
- `2026-06-30` живой прогон уже разделил проблему по слоям:
  - сам робот подтверждённо жив в домашней сети и с рабочего компьютера отвечает на `192.168.1.159:8080`;
  - runtime панели тоже несёт `board_ip = 192.168.1.159`;
  - на телефоне в этот момент активен VPN `run.nlab.iproxy` / `org.amnezia.vpn`, из-за чего приложение не может достучаться до локального адреса робота;
  - в Android-клиент уже добавлены:
    - `Wi‑Fi`-привязанный `HTTP`-клиент для локальной проверки;
    - подхват `board_ip` из панели как быстрой подсказки для локального поиска;
    - честные статусы в `MENU` и на шаге повторного `Wi‑Fi`, которые прямо говорят, что локальный поиск сейчас блокирует VPN.
- `2026-07-01` живой прогон на `TECNO LI9` подтвердил следующую точку:
  - системная геолокация на телефоне обязана быть включена, иначе приложение не видит свежий `Wi‑Fi`-скан и слишком долго держит устаревшие состояния;
  - после включения геолокации `MENU` начал честно видеть рядом сеть робота `GOSHA-A-1BE1`;
  - Android-диалог на временное подключение к сети робота открывается штатно;
  - после выбора `GOSHA-A-1BE1` встроенный `HotspotPortalActivity` снова открывает локальный портал прямо внутри приложения;
  - живой журнал подтвердил успешный выход из портала:
    - страница дошла до `done.html`;
    - зафиксировано `Exit config mode request sent`;
    - робот вышел из точки доступа;
  - после возврата телефона в домашний `Wi‑Fi` приложение уже нашло робота локально по адресу `192.168.0.102`;
  - верхний блок `MENU` теперь больше не врёт статичным текстом `Робот на связи`, а зависит от реального состояния;
  - если робот уже принял настройки, но телефон ещё не вернулся в домашний `Wi‑Fi`, приложение теперь прямо просит вернуть телефон в домашнюю сеть, а не показывает расплывчатое «ищу адрес».
- `2026-07-02` внесён прикладной сетевой фикс:
  - `RobotWifiConnector` больше не пересаживает весь процесс приложения на сеть робота через `bindProcessToNetwork(...)`;
  - сеть робота теперь используется только для адресных локальных запросов;
  - панель и другой внешний контур должны снова идти по обычному интернет-маршруту телефона, а не застревать в `192.168.4.x`.
- `2026-07-02` поверх этого сразу подтверждён живой результат на том же телефоне:
  - домашний `Wi‑Fi`: `AX3000T-4039`;
  - активный VPN на телефоне: `run.nlab.iproxy`;
  - робот локально доступен по `192.168.1.159`;
  - после правки `MainActivity` приложение больше не прекращает локальный поиск только из-за VPN;
  - живой журнал показал:
    - `discoverRobotLocally(subnet=192.168.1, preferred=[192.168.1.159])`;
    - успешное нахождение `192.168.1.159`;
    - `reportPresenceAsync success: state=home_wifi_local localHost=192.168.1.159`;
  - на экране `MENU` подтверждено честное состояние:
    - `Робот в сети`;
    - `Локальный адрес: 192.168.1.159`;
  - `shared_prefs` на телефоне уже обновлены:
    - `robot_host = 192.168.1.159`;
    - `connector_robot_host = 192.168.1.159`;
  - runtime панели после этого несёт:
    - `mobile_presence.state = home_wifi_local`;
    - `mobile_presence.local_host = 192.168.1.159`;
    - `connectivity.connected = true`;
    - `connectivity.evidence = local_host`;
  - отдельная выдержка `MENU` больше 60 секунд пройдена без самопроизвольного отката назад.
- `2026-07-02` следующим живым прогоном закрыт скрытый дефект Android-локального поиска:
  - робот и панель уже были исправны, но ручной проверочный обмен запрос-ответ по `ws://<host>:8080/ws` внутри `LocalRobotDiscovery` выполнялся в главном потоке;
  - из-за `NetworkOnMainThreadException` приложение затягивало проверку и могло ложно не подтверждать живого робота;
  - теперь ручной `ws`-проверочный обмен вынесен в рабочий поток;
  - дополнительно добавлены:
    - быстрая приоритетная проверка сохранённого `robotHost`;
    - быстрый TCP-предфильтр `:8080` перед полным `ws`-проверочным обменом;
  - на живом телефоне после этого подтверждено:
    - `MENU` быстро возвращается в состояние `Робот в сети`;
    - локальный адрес `192.168.1.159` снова сохраняется в `shared_prefs`;
    - `ConnectorForegroundService` удерживается дольше 3 минут после сворачивания приложения;
    - runtime панели остаётся свежим с:
      - `mobile_presence.state = home_wifi_local`;
      - `mobile_presence.local_host = 192.168.1.159`;
      - `connectivity.connected = true`.
- `2026-05-27` подтверждено на живом телефоне:
  - лишние старые пакеты удалены;
  - на устройстве оставлен только `com.maxcorp.gosha.mobile`;
  - после нажатия `Подключить робота` приложение действительно открывает встроенный `HotspotPortalActivity`;
  - внутри приложения реально загружается страница робота `192.168.4.1`;
  - телефон остаётся в сети `GOSHA-A-1BE1`, а не отваливается обратно в домашний `Wi‑Fi`;
  - на живых логах подтверждены `200 OK` для `/`, `/saved/list`, `/scan`, `/advanced/config`.
- Для этого в `RobotWifiConnector.bindToCurrentRobotWifi()` уже сделан прикладной фикс:
  - повторное привязывание к сети робота больше не вызывает преждевременный `release()` активного `NetworkRequest`;
  - активная сеть робота переиспользуется, если она уже удерживается приложением.
- Локальная Android-проверка этой точки уже подтверждена:
  - `./gradlew --no-daemon testClientDebugUnitTest`
  - `./gradlew --no-daemon assembleClientDebug`
  - `./gradlew --no-daemon lintClientDebug`
- Обновление `2026-07-10` по живому входу через `AI_OFFICE`:
  - офисная задача: `task-20260709T172634Z-android-wi-fi`;
  - рабочее дерево: `/home/max/worktrees/gosha/mobile-portal`;
  - ветка: `feature/mobile-hotspot-portal`, базовый верхний коммит `7313570`;
  - на `TECNO LI9` полностью пройдено `MENU -> новая Wi-Fi сеть -> GOSHA-A-1BE1 -> встроенный портал -> done.html -> домашний Wi-Fi -> MENU`;
  - исправлена гонка, при которой проверка после портала освобождала сеть робота до закрытия `HotspotPortalActivity`;
  - системный выбор Android искал строку `GOSHA-A-1BE1` примерно `49` секунд, но показывал индикатор, а приложение не выдавало ложный готовый статус;
  - после `done.html` робот найден локально по `192.168.1.159`, панель получила свежий `mobile_presence = home_wifi_local`, отката из `MENU` не было;
  - причина прежней потери сигнала после сворачивания — OEM-механизм `Transsion Hiber`, а не остановка `ConnectorForegroundService`;
  - режим `Гоша -> Нет ограничений` в `BatteryLab` подтвердил свежий сигнал до `T+195`, тот же PID и отсутствие новой заморозки;
  - обычный `deviceidle whitelist` и одно только разрешение уведомлений эту OEM-заморозку не устраняют;
  - в клиент добавлены запрос уведомлений, точная OEM-инструкция, постоянные кнопки настроек и корректное закрытие проверочного WebSocket;
  - финальный APK установлен, SHA-256 `d353aafb47491f443c8290f999b6827ef1044077cbb189623fec872655e60ec7`;
  - `testClientDebugUnitTest`, `assembleClientDebug`, `lintClientDebug` успешны.
- Обновление `2026-07-11` по P0 локальных Android-`WebSocket`-сеансов:
  - `AI_OFFICE`-задача: `task-20260711T063743Z-p0-android-tecno-li9-gosha-main`;
  - рабочая ветка остаётся `feature/mobile-hotspot-portal`, базовый коммит до новых изменений `bd072a8`;
  - добавлены единый координатор локальных сеансов, честные состояния `executed / skipped / stale`, измеряемые счётчики и адаптивный интервал 10–60 секунд;
  - raw WebSocket handshake удалён: используется `OkHttp`, корректный close-frame, окно закрытия 1500 мс и правильный проброс отмены;
  - экран может переиспользовать только свежий успешный результат службы не старше 5 секунд и только для совпадающего сохранённого адреса в текущей подсети;
  - это живьём устранило гонку, в которой служба видела робота, а следующий ручной сеанс через 9,5 секунды ложно возвращал `robot not found`;
  - итоговый APK установлен и совпал с артефактом, SHA-256 `66e18603d942583506c6d45c1bc4c40b2303ca68f8ce90f880e437c25e6729e4`;
  - `testClientDebugUnitTest`, `assembleClientDebug`, `lintClientDebug` успешны;
  - после сворачивания 234 секунды сохранялись PID `25464`, `isForeground=true`, успешные фоновые проверки и свежий сигнал панели;
  - панель видела `home_wifi_local` и `local_host = 192.168.1.159`;
  - живой журнал: `/home/max/AI_OFFICE/local_only/ai-office/logs/task-20260711T063743Z-p0-android-tecno-li9-gosha-main/20260711-66e18603-final3-adb-260s.log`;
  - для чистых следующих прогонов не использовать `nc -z` на порту робота `:8080`: это raw TCP-подключение само искажает состояние односессионного `WSControl`.
  - финальный reviewer не нашёл P0; оставил P1 по контракту платформы: старый `transport_state` читает `robot_ws_ok`, но пока не учитывает `robot_ws_probe_state=skipped` и возраст кеша; `mobile_presence` уже защищён.
- Дополнительно подтверждено:
  - страница робота рендерится именно во встроенном `WebView`, а не только в стороннем браузере;
  - запасной белый экран больше не воспроизводится в текущем рабочем проходе открытия страницы.
- Следующий живой шаг после этой серии правок:
  - снова перевести робота в режим подключения новой точки доступа;
  - пройти путь `MENU -> Подключение робота к Wi‑Fi -> Подключить -> done.html -> возврат`;
  - промерить реальные задержки:
    - сколько занимает поиск состояния на главном меню;
    - сколько занимает нажатие `Подключить`;
    - где именно нужен явный статус загрузки вместо ощущения зависания;
  - после успешного возврата дополнительно проверить:
    - повторяется ли уже подтверждённая долгая живость сигнала панели после нового полного цикла;
    - не теряется ли `mobile_presence` после сворачивания приложения;
    - стабильно ли живёт `ConnectorForegroundService`.
- Для iOS основной дальнейший контур теперь отдельный:
  - `GOSHA_MOBILE_IOS`
- iOS-каркас уже доведён до реального Xcode-проекта и первого запуска в симуляторе.
- Живой запуск iOS на подключённом iPhone сейчас заблокирован несовместимостью:
  - на машине `Xcode 13.2.1`
  - на телефоне `iOS 26.1`
- Нужно проверить и дополировать локальный портал `192.168.4.1`.
- Нужно сделать чуть понятнее тексты про то, найден робот локально, через панель или ещё не подтверждён.
- Нужно живьём перепроверить новую ветку "подтверждён только через платформу, локальный адрес ещё уточняется".
- Нужно отдельно держать в голове, что часть претензий к тембру голоса идёт не из мобильного клиента, а из серверного `TTS`-движка платформы.

## Что делать следующим

1. Работать из `/home/max/worktrees/gosha/mobile-portal`, а не из `main`.
2. Не переносить отвергнутый общий ограничитель громкости `30` из `/home/max/worktrees/gosha/firmware-wifi-config-volume`.
3. P0 Android-`WebSocket` закрыт; не возвращать параллельные локальные сеансы и не публиковать presence из `skipped / stale`.
4. Выполнить ещё два подряд полных цикла `MENU -> портал -> done.html -> MENU` и в каждом проверить задержки, `local_host`, `ConnectorForegroundService` и свежесть `mobile_presence` после 3 минут.
5. Затем остановить Android-коннектор и провести чистую аппаратную диагностику:
   - подтвердить активный OTA-раздел по загрузочному UART-журналу;
   - независимо получить `ota.label` через локальный `self.get_system_info`;
   - сравнить первую и повторную Wi-Fi-подсказку по `wifi_prompt_diag`.
6. Исправить известное ограничение OEM-подсказки: `Позже` или открытие настроек не должно навсегда считаться подтверждением режима `Нет ограничений`.
7. Отдельной платформенной задачей научить операторскую диагностику учитывать `robot_ws_probe_state` и возраст кеша.
8. Для iOS:
   - перейти в отдельный репозиторий `GOSHA_MOBILE_IOS`;
   - взять совместимый с `Xcode 13.2.1` iPhone/iOS или перейти на более новый Mac/Xcode;
   - затем открыть `GoshaMobileIOS.xcodeproj` в Xcode;
   - настроить signing;
   - пройти живой сценарий на iPhone.
9. Если появятся новые локальные `AGENTS.md`, держать их в том же русском техническом стиле.

# PROJECT STATUS

## Сделано

- В `GOSHA_MOBILE` унифицированы правила общения агента:
  - корневой и локальные `AGENTS.md` теперь явно требуют понятный русский технический язык;
  - запрещён русско-английский суржик в обычном тексте;
  - для типовых слов зафиксированы русские формы и пояснения.
- Для новых чатов зафиксирован обязательный протокол входа:
  - агент обязан проверять checkpoint-документы, текущую ветку и верхний коммит;
  - в первом содержательном ответе обязан явно назвать ветку, стадию и следующий приоритетный шаг.
- Для параллельной ручной разработки зафиксирована отдельная политика `git worktree`:
  - ручные рабочие деревья выносятся в `/home/max/worktrees/gosha/<контур>-<задача>`;
  - новые мобильные ветки по умолчанию ответвляются от `main`;
  - flow подключения, runtime-диагностика и `RuStore`-подготовка не должны смешиваться в одной ветке без явной причины.
- Создан отдельный локальный проект `GOSHA_MOBILE`.
- Проект инициализирован как самостоятельный git-репозиторий.
- Для `GOSHA_MOBILE` уже настроен удалённый `origin`:
  - `git@github.com:MaxCorpOrg/GOSHA_MOBILE.git`
- Выполнен первый `push` в GitHub:
  - ветка `main` уже отслеживает `origin/main`
- Публичный репозиторий мобильного клиента теперь доступен по адресу:
  - `https://github.com/MaxCorpOrg/GOSHA_MOBILE`
- Исходники взяты одноразовым копированием из старого Android-клиента `AI_ROBOT`.
- Общая карта всех связанных контуров зафиксирована в:
  - `/home/max/GOSHA_PLATFORM/docs/GOSHA_PROJECT_MAP_RU.md`
- Новый канонический идентификатор приложения:
  - `com.maxcorp.gosha.mobile`
- В проекте оставлен только клиентский вариант сборки.
- Отдельный административный вариант исключён из первого этапа.
- Базовые адреса по умолчанию переведены на контур `GOSHA_PLATFORM`:
  - панель `18876`
  - серверный контур `18080`
- Клиентский код начал читать новый серверный раздел `mobile_profile` из пакета подключения.
- Пользовательский слой переведён на бренд `Гоша`.
- Локальный поиск робота оставлен без изменения:
  - `ws://<ip>:8080/ws`
- Приложение теперь технически принимает оба префикса сети робота:
  - `GOSHA-`
  - `Xiaozhi-`
- Пользовательские тексты переводятся на `GOSHA-`.
- Android-исходники пока ещё лежат в старом пути:
  - `app/src/main/java/com/maxcorp/edgeconnector`
  Это технический след копирования, а не продуктовый бренд.
- Локально подтверждена самостоятельная сборка проекта:
  - `assembleClientDebug`
  - `testClientDebugUnitTest`
- Выполнен живой прогон приложения на телефоне `TECNO LI9` через `adb`.
- На живом телефоне подтверждено:
  - установка `app-client-debug.apk`
  - успешный ввод кода `MJ6SG97A`
  - переход на шаг `Подключение робота к Wi‑Fi`
  - сохранение нового серверного контура в `shared_prefs/gosha_mobile_prefs.xml`:
    - `panel_url = http://151.241.228.232:18876`
    - `hub_url = ws://151.241.228.232:18080/mcp/`
    - `cloud_endpoint = ws://151.241.228.232:18080/mcp/?token=...&robot_id=gosha-main`
    - `mobile_websocket_url = ws://151.241.228.232:18080/xiaozhi/v1/`
    - `robot_id = gosha-main`
    - `robot_wifi_prefixes = GOSHA-,Xiaozhi-`
  - выдача разрешений на местоположение и устройства поблизости
  - системный выбор и подключение к сети `GOSHA-A-1BE1`
  - открытие внутреннего экрана `HotspotPortalActivity`
- Исправлен важный UX-дефект честности связности:
  - мобильный клиент больше не считает робота "подключённым через панель" только по наличию настроенного `control`-контура
  - `PanelApiClient.fetchRobotRuntime()` теперь читает новый блок `connectivity` из `GOSHA_PLATFORM`
  - Android теперь различает:
    - подтверждённый live-probe;
    - свежий self-hosted след устройства;
    - локальный host;
    - отсутствие живых подтверждений
- Исправлен ещё один UX-дефект честности после подключения:
  - если панель подтверждает робота, но ещё не прислала `local_host`, приложение больше не говорит так, будто робот уже найден прямо в локальной сети
  - для такого случая Android теперь честно показывает:
    - робот уже на связи с платформой;
    - локальный адрес не подтверждён;
    - повторная локальная проверка произойдёт при следующем входе или возобновлении меню
  - diagnostics-слой теперь различает:
    - локально найденный робот;
    - подтверждение только через платформу без локального адреса
- В diagnostics-слое приложения теперь видно:
  - `evidence`
  - `lastSeen`
  Это помогает понять, почему панель считает робота доступным или недоступным.
- Локально подтверждён живой runtime-контракт:
  - `GET /api/mobile/robots/<robot_id>/runtime`
  - для непривязанного `gosha-main` локальная лаборатория уже честно отдаёт:
    - `connected=false`
    - пустой `evidence`
    - `fresh_device_contact=false`
- Подтверждён канонический Android-пакет проверок после этой правки:
  - `./gradlew --no-daemon clean assembleClientDebug testClientDebugUnitTest lintClientDebug`
  - запускать эти задачи нужно последовательно одним вызовом, а не параллельно, чтобы не получать ложные сбои в `build/`
- В `GOSHA_MOBILE` добавлен отдельный iOS-каркас:
  - `ios/GoshaMobileIOS`
- Для iOS уже создан отдельный GitHub-репозиторий:
  - `https://github.com/MaxCorpOrg/GOSHA_MOBILE_IOS`
- Локальный отдельный рабочий корень iOS:
  - `/Users/maksim/Developer/GOSHA_MOBILE_IOS`
- iOS-каркас переведён на новый контракт `GOSHA_PLATFORM`:
  - адреса `18876/18080`
  - `bundle.mobile_profile`
  - runtime `connectivity`
  - основной префикс `GOSHA-`
  - переходный префикс `Xiaozhi-`
- Для iOS локально собран совместимый `XcodeGen 2.30.0`:
  - `/Users/maksim/bin/xcodegen`
- Через него уже сгенерирован реальный Xcode-проект:
  - `ios/GoshaMobileIOS/GoshaMobileIOS.xcodeproj`
- Для iOS уже подтверждена локальная проверка:
  - `xcrun swiftc -typecheck -sdk ...`
- И уже подтверждена сборка проекта через `xcodebuild` для симулятора без signing:
  - `xcodebuild -project ... -scheme GoshaMobileIOS -sdk iphonesimulator ... build`
- В `Info.plist` iOS-каркаса уже зафиксирован явный runtime-конфиг:
  - `GoshaPanelBaseURL`
  - `GoshaPrivacyPolicyURL`
  - `GoshaTermsOfUseURL`
  - `GoshaRobotPortalURL`
  - `GoshaRobotSSIDPrefixes`
- Для dev-контура в iOS уже добавлены нужные сетевые разрешения:
  - `ATS`-исключение для `151.241.228.232:18876`
  - `ATS`-исключение для `192.168.4.1`
  - `NSLocalNetworkUsageDescription`
- Для iOS уже подтверждён симуляторный install/launch:
  - `xcrun simctl install booted .../Гоша.app`
  - `xcrun simctl launch booted com.maxcorp.gosha.mobile.ios`
- Для локального сопровождения обоих репозиториев добавлен безопасный sync-механизм:
  - `/Users/maksim/bin/gosha_repo_sync.sh`
  - `launchd` job `com.maxcorp.gosha-repo-sync`
  - job делает `fetch --all --prune` и выполняет `pull --ff-only` только на чистой ветке без локальных коммитов
  - текущий отчёт сохраняется в:
    - `/Users/maksim/Developer/.gosha-sync/last_report.txt`

## На чем остановились

- По языковым правилам работа уже выровнена:
  - агент должен писать понятным русским техническим языком;
  - английский оставляется только в командах, путях, названиях файлов, функций, переменных, веток, коммитов и в коде.
- `GOSHA_MOBILE` уже отделён от старого проекта и подтверждён локальной сборкой.
- Новый пакет подключения из `GOSHA_PLATFORM` уже корректно сохраняется в настройках приложения.
- Подтверждено, что приложение не зависит от старого клиентского контура `8876/8890`.
- Исправлен ложный сценарий "подключено через панель без живого сигнала".
- Главный оставшийся мобильный хвост теперь уже не в белом экране и не в самом открытии локального портала.
- Текущая реальная незакрытая граница смещена дальше:
  - полный путь `системный выбор сети робота -> встроенный портал -> выход робота из точки доступа -> возврат в MENU` уже подтверждён на живом телефоне;
  - основной оставшийся риск теперь в том, как приложение сообщает состояние в панель администратора, пока процесс ещё привязан к сети робота без внешнего интернета.
- `2026-05-26` начата отдельная рабочая ветка `feature/mobile-hotspot-portal` для этого хвоста.
- В этой ветке сначала была проверена гипотеза с обходом `WebView`, но на живом устройстве она не дала надёжного рабочего результата как основной пользовательский путь.
- `2026-05-27` живой прогон на телефоне `TECNO LI9` уточнил реальную границу проблемы:
  - с телефона удалены старые пакеты `com.maxcorp.edgeconnector` и `com.maxcorp.edgeconnector.admin`;
  - на устройстве оставлен только актуальный пакет `com.maxcorp.gosha.mobile`;
  - найден и закрыт мобильный дефект удержания сети робота:
    - `RobotWifiConnector.bindToCurrentRobotWifi()` раньше вызывал `release()` и рвал активный `NetworkRequest`;
    - из-за этого телефон успевал выскочить из `GOSHA-A-1BE1` обратно в домашний `Wi‑Fi` до загрузки портала;
    - после правки телефон остаётся в `GOSHA-A-1BE1`, а `HotspotPortalActivity` открывается штатно.
- Тем же днём уточнено и следующее:
  - локальный портал робота сам по себе живой и открывается в другом приложении и в системном браузере;
  - реальная проблема была не в роботе, а в том, что приложение не находило правильный `Network` робота внутри `ConnectivityManager`;
  - поиск сети робота по одному `SSID` на `TECNO LI9` оказался недостаточным, поэтому дополнительно включён поиск по локальной подсети `192.168.4.x`.
- Поздний живой прогон `2026-05-27` уже подтвердил новую рабочую точку:
  - `MainActivity.startRobotWifiPortal()` снова ведёт во встроенный `HotspotPortalActivity`, а не во внешний браузер;
  - `RobotWifiConnector.bindToCurrentRobotWifi()` успешно привязывает процесс к сети робота `711`;
  - `RobotPortalClient` получает `200 OK` для `http://192.168.4.1` и возвращает HTML страницы;
  - внутри приложения подтверждены рабочие запросы `/saved/list`, `/scan`, `/advanced/config`;
  - встроенный `WebView` снова показывает страницу робота со списком сетей и формой подключения.
- В этой же ветке после рабочего портала уже собран отдельный слой пост-регистрационной стабильности:
  - в локальном состоянии появился флаг `setupCompleted`;
  - после успешного возврата в `MENU` приложение больше не должно само открывать шаг переподключения из-за кратковременной нестабильности сети или запаздывания панели;
  - добавлен тихий фоновый контур проверки `Wi‑Fi` / панели / локального адреса без смены экрана;
  - мобильный клиент теперь сам отправляет в панель `mobile_presence` со значениями `phone_on_robot_wifi`, `robot_hotspot_visible`, `home_wifi_local`, `not_found`;
  - `ConnectorForegroundService` зарегистрирован в `AndroidManifest`, оформлен под Android 14 как `dataSync` и должен автоматически запускаться, когда появляется реальный `robotHost`.
- Локальная проверка кода уже пройдена:
  - `./gradlew --no-daemon testClientDebugUnitTest`
  - `./gradlew --no-daemon assembleClientDebug`
  - `./gradlew --no-daemon lintClientDebug`
- Поверх этой точки `2026-05-27` была развёрнута следующая серия Android-правок:
  - ускорение статусов в `MENU`;
  - автоуточнение состояния робота в шаге повторного `Wi‑Fi`;
  - ускоренный локальный поиск через сохранённый `robotHost`;
  - уменьшенные паузы после `submit` и после возврата из локального портала.
  - основные файлы этой серии:
    - `app/src/main/java/com/maxcorp/edgeconnector/MainActivity.kt`
    - `app/src/main/java/com/maxcorp/edgeconnector/LocalRobotDiscovery.kt`
    - `app/src/main/java/com/maxcorp/edgeconnector/HotspotPortalActivity.kt`
    - `app/src/main/res/values/strings.xml`
    - `app/src/main/java/com/maxcorp/edgeconnector/ConnectorConfig.kt`
    - `app/src/test/java/com/maxcorp/edgeconnector/ConnectorConfigTest.kt`
- `2026-06-30` живой прогон уже уточнил главный оставшийся блокер:
  - робот действительно жив в домашней сети и с рабочего компьютера отвечает на `192.168.1.159:8080`;
  - runtime панели тоже хранит `board_ip = 192.168.1.159`;
  - на телефоне в момент проверки активен VPN `run.nlab.iproxy` / `org.amnezia.vpn`, из-за чего локальный маршрут до робота с телефона не проходит;
  - поэтому текущая проблема смещена с робота и панели на сетевую политику телефона;
  - в Android-клиент уже добавлены:
    - `Wi‑Fi`-привязанный `HTTP`-клиент для локальных обращений;
    - подхват `board_ip` из runtime панели как подсказки для быстрого поиска;
    - честные статусы в `MENU` и в шаге повторного `Wi‑Fi`, которые прямо говорят о блокировке локальной проверки активным VPN.
- `2026-07-01` живой прогон на `TECNO LI9` подтвердил новую рабочую точку:
  - найден ещё один обязательный мобильный фактор:
    - если на телефоне выключена системная геолокация, приложение не получает свежие результаты `Wi‑Fi`-сканирования и держит устаревшие состояния слишком долго;
  - после включения геолокации подтверждено:
    - `MENU` честно видит рядом сеть `GOSHA-A-1BE1`;
    - Android-диалог на временное подключение к сети робота открывается штатно;
    - после выбора `GOSHA-A-1BE1` встроенный `HotspotPortalActivity` снова открывает локальный портал прямо внутри приложения;
    - портал доходит до `done.html`, а в живом журнале фиксируется `Exit config mode request sent`;
    - после этого робот действительно выходит из своей точки доступа;
  - подтвержден и полный возврат:
    - после возврата телефона в домашний `Wi‑Fi` приложение нашло робота локально по адресу `192.168.0.102`;
    - верхний блок `MENU` показал честное состояние `Робот в сети и готов к работе`;
  - параллельно закрыты дополнительные мобильные хвосты:
    - убран ложный статичный верхний статус `Робот на связи`;
    - исправлено определение `SSID` и подсети, чтобы при выключенном `Wi‑Fi` приложение не делало вид, что просто не нашло робота;
    - добавлен честный промежуточный статус для случая, когда робот уже принял настройки, но телефон ещё не вернулся в домашний `Wi‑Fi`;
  - новый реальный остаточный дефект теперь уже сетевой:
    - пока приложение привязано к сети робота `192.168.4.x`, отправка `mobile_presence` и чтение runtime панели могут уходить в таймаут, потому что у точки доступа робота нет внешнего интернета.
- `2026-07-02` поверх этого внесён точечный сетевой фикс:
  - `RobotWifiConnector` больше не переводит весь процесс приложения на сеть робота через `bindProcessToNetwork(...)`;
  - сеть робота остаётся только удерживаемым локальным маршрутом для портала и локального `ws://<robot>:8080`;
  - внешний трафик в панель и другие интернет-адреса должен снова идти по обычному интернет-маршруту телефона;
  - локальная кодовая проверка этой правки уже подтверждена:
    - `./gradlew --no-daemon assembleClientDebug testClientDebugUnitTest`
- `2026-07-02` сразу после этого подтверждён живой Android-результат:
  - на `TECNO LI9` телефон был в домашнем `Wi‑Fi` `AX3000T-4039`, а VPN `run.nlab.iproxy` оставался включён;
  - робот подтверждённо жил в сети по `192.168.1.159`;
  - `MainActivity` был доработан так, чтобы не прерывать локальный поиск только из-за VPN, потому что сам поиск уже идёт через `Wi‑Fi`-привязанный клиент;
  - одновременно убрана ложная отправка `mobile_presence = not_found` из VPN-веток;
  - живой журнал подтвердил:
    - локальный поиск `discoverRobotLocally(subnet=192.168.1, preferred=[192.168.1.159])`;
    - нахождение `192.168.1.159`;
    - успешную отправку `home_wifi_local`;
  - после этого на телефоне обновились:
    - `robot_host = 192.168.1.159`;
    - `connector_robot_host = 192.168.1.159`;
  - runtime панели стал показывать:
    - `mobile_presence.state = home_wifi_local`;
    - `mobile_presence.local_host = 192.168.1.159`;
    - `connectivity.connected = true`;
    - `connectivity.evidence = local_host`;
  - отдельная выдержка `MENU` дольше 60 секунд пройдена без самопроизвольного возврата назад.
- `2026-07-02` следующим живым прогоном закрыт ещё один скрытый Android-дефект:
  - робот и панель уже были исправны, но ручной проверочный обмен запрос-ответ по `ws://<host>:8080/ws` внутри `LocalRobotDiscovery` выполнялся в главном потоке;
  - из-за `NetworkOnMainThreadException` приложение затягивало локальную проверку и могло ложно не подтверждать живого робота, хотя `192.168.1.159:8080` уже отвечал;
  - этот проверочный обмен вынесен в рабочий поток;
  - в локальный поиск дополнительно добавлены:
    - приоритетная быстрая проверка сохранённого `robotHost`;
    - быстрый TCP-предфильтр `:8080`;
    - отдельный ручной `ws`-проверочный обмен уже вне главного потока;
  - после этого живьём подтверждено:
    - `MENU` снова быстро приходит к честному состоянию `Робот в сети`;
    - `robot_host` и `connector_robot_host` закрепляются как `192.168.1.159`;
    - `ConnectorForegroundService` живёт дольше 3 минут после сворачивания приложения;
    - runtime панели остаётся свежим и продолжает показывать:
      - `mobile_presence.state = home_wifi_local`;
      - `mobile_presence.local_host = 192.168.1.159`;
      - `connectivity.connected = true`.
- Для iOS новый ближайший блокер уже другой:
  - каркас есть, `.xcodeproj` уже сгенерирован, `xcodebuild` проходит и запуск в iOS Simulator подтверждён;
  - iOS-код уже выделен в отдельный репозиторий, поэтому дальнейшая iOS-разработка может идти отдельно от Android-клиента;
  - живой прогон на реальном iPhone сейчас упирается не в код приложения, а в toolchain-совместимость;
  - на этой машине стоит `Xcode 13.2.1`, а подключённый iPhone на `iOS 26.1` не поддерживается этим Xcode, поэтому device run и signing-проверка здесь пока заблокированы
- Дополнительно уже проверена hack-ветка с подменой `DeviceSupport/DDI`:
  - временная подложка `26.1 -> 15.2` и `26.1 (23B85) -> 15.2` убрала ошибку `Could not locate device support files`
  - после этого Xcode действительно дошёл до mount `DeveloperDiskImage`
  - но упёрся в `AMDeviceMountImage` / `0xE8000007` / `The argument is invalid`
  - значит простой alias/symlink не решает задачу
  - для следующей попытки нужен уже реальный `DeveloperDiskImage` под `26.1 (23B85)`
  - временные ссылки после проверки уже удалены
- Отдельно проверен встроенный системный путь:
  - `MobileDeviceUpdater` на Big Sur запускается и делает `SU query` по тегу `DEVICESUPPORT`
  - но по текущим логам не видно, что он нашёл и скачал подходящий asset для этого устройства
- На текущей точке уже подтверждено на живом телефоне:
  - автоматическое открытие встроенного портала после системного выбора сети робота;
  - завершение шага подключения в самом портале;
  - возврат приложения в `MENU` без самопроизвольного отката;
  - автоматическое нахождение локального `robotHost` после возврата телефона в домашний `Wi‑Fi`;
  - честный статус `MENU` для промежуточного случая, когда телефон ещё не вернулся в домашнюю сеть;
  - локальное обнаружение робота даже при активном VPN на телефоне;
  - свежее обновление панели через `mobile_presence = home_wifi_local`.
- `2026-07-09` выполнен новый вход через `AI_OFFICE` для следующего живого Android Wi-Fi цикла:
  - офис поднят штатно, `verify_local_office.sh` вернул `ready: true`;
  - через `/office start` создана командная задача:
    - `task-20260709T172634Z-android-wi-fi`;
    - тред: `http://127.0.0.1:8065/ai-office/pl/3mba5r15cinntquy4mirf49r5w`;
  - `manager` завершился состоянием `manager_completed`;
  - `/office plan task-20260709T172634Z-android-wi-fi` показывает одну дочернюю задачу для `coder`;
  - план не утверждён до подключения телефона, чтобы дочерний исполнитель не упёрся в пустой `adb`;
  - после появления устройства можно продолжить через:
    - `/office approve-plan task-20260709T172634Z-android-wi-fi`;
  - подтверждены Android-точки входа:
    - рабочее дерево `/home/max/worktrees/gosha/mobile-portal`;
    - ветка `feature/mobile-hotspot-portal`;
    - верхний коммит `7313570 Стабилизировать локальный Android-сигнал и обновить checkpoint`;
  - `adb` в обычном `PATH` не найден, но на машине есть:
    - `/home/max/Android/Sdk/platform-tools/adb`;
  - подключённых Android-устройств нет:
    - `/home/max/Android/Sdk/platform-tools/adb devices -l` возвращает пустой список;
    - `lsusb` не показывает телефон;
  - поэтому живой Wi-Fi цикл, установка APK и `adb logcat` в этом входе физически заблокированы до подключения или авторизации телефона;
  - актуальный `clientDebug` APK собран успешно:
    - `./gradlew --no-daemon assembleClientDebug`;
    - `app/build/outputs/apk/client/debug/app-client-debug.apk`.
- `2026-07-10` этот цикл фактически завершён на `TECNO LI9`:
  - телефон `119022547O005961`, системная геолокация включена, VPN выключен;
  - пройден путь `MENU -> GOSHA-A-1BE1 -> встроенный портал -> done.html -> домашний Wi-Fi -> MENU`;
  - после возврата робот найден по `192.168.1.159`, панель получила `mobile_presence = home_wifi_local` и `local_host = 192.168.1.159`;
  - отката из `MENU` не было;
  - после настройки `TECNO BatteryLab -> Нет ограничений` один PID, foreground-служба и свежий сигнал панели подтверждены до `T+195`;
  - точный установленный APK совпал с артефактом сборки, SHA-256 `d353aafb47491f443c8290f999b6827ef1044077cbb189623fec872655e60ec7`;
  - исправлена гонка возврата из `HotspotPortalActivity` и добавлено корректное закрытие проверочной WebSocket-сессии;
  - диагностическая прошивка дала две нормальные по громкости подсказки, но чистое сравнение не получено: UART содержал интенсивный поток `WSControl`, а `wifi_prompt_diag` отсутствовал.
- `2026-07-11` через `AI_OFFICE` закрыт P0 фоновых локальных `WebSocket`-проверок Android:
  - командная задача: `task-20260711T063743Z-p0-android-tecno-li9-gosha-main`;
  - добавлен единый координатор локальных сеансов для `MainActivity`, `ConnectorForegroundService` и функциональных команд;
  - фоновая проверка различает реально выполненный, пропущенный и устаревший результат; `home_wifi_local` публикуется только после реально выполненной успешной проверки;
  - интервал после ошибок растёт до 60 секунд, успешная проверка возвращает базовый интервал 10 секунд;
  - локальное обнаружение сначала параллельно проверяет TCP, но подтверждает найденные адреса последовательными `WebSocket`-сеансами;
  - raw-проверка `LocalWsHandshakeProbe` заменена на `OkHttp WebSocket` с корректным `close(1000, "probe-ok")`, окном закрытия 1500 мс и пробросом отмены coroutine;
  - гонка «service success -> повторный ручной WebSocket -> robot not found» устранена переиспользованием только свежего, не старше 5 секунд, успешного результата службы для того же сохранённого адреса и той же подсети;
  - установочный APK совпал со сборкой, SHA-256 `66e18603d942583506c6d45c1bc4c40b2303ca68f8ce90f880e437c25e6729e4`;
  - `testClientDebugUnitTest`, `assembleClientDebug` и `lintClientDebug` завершились успешно;
  - основной 260-секундный журнал содержит 17 реально выполненных проверок, все `ok=true`; пропущенные и устаревшие попытки не выдавали ложный успешный сигнал;
  - после `HOME` один PID `25464`, foreground-служба и свежий `mobile_presence = home_wifi_local` с `local_host = 192.168.1.159` сохранились 234 секунды;
  - более поздний post-check дошёл до счётчика `executed=56` без единого `executed ok=false`;
  - журналы: `20260711-66e18603-final3-adb-260s.log` и `20260711-66e18603-final3-postcheck.log` в папке задачи AI_OFFICE.
- `2026-07-11` по задаче `task-20260711T103436Z-2-done-html-14-29-06-wi-fi-14-2` исправлен узкий дефект возврата после `done.html`:
  - пост-портальный цикл больше не завершает сценарий сразу по сигналу панели `CONNECTED_VIA_PANEL`, если локальный адрес ещё не подтверждён;
  - такой сигнал теперь только запоминается и передаёт `localHostHint` / `board_ip` в следующие локальные проверки;
  - после возврата телефона в домашний `Wi‑Fi` приложение продолжает ограниченный цикл `discoverRobotLocally(...)` и быстро проверяет подсказанный адрес;
  - если локальный адрес так и не подтвердился после лимита попыток, `MENU` показывает конечный честный статус: робот на связи с платформой, но локальный адрес не подтверждён;
  - внешний `nc` и внешние `WebSocket`-сеансы к `:8080` для этой правки не используются.
  - локальная проверка кода подтверждена:
    - `ANDROID_HOME=/home/max/Android/Sdk ./gradlew --no-daemon testClientDebugUnitTest assembleClientDebug lintClientDebug`.
- `2026-07-11` исправление подтверждено отдельным полным циклом №3:
  - исправленный APK и установленный пакет совпали, SHA-256 `ed6a97dc1c4bc60aa334df17ff8bf18269e3c6aad79a6dbd51ab6b8c84ecbe09`;
  - `POST /submit` зафиксирован в `16:05:51`, `done.html` — в `16:05:55`, домашний `Wi-Fi` — в `16:06:00`;
  - локальный поиск автоматически нашёл `gosha-main` по `192.168.1.159` в `16:06:20`, без ручного открытия статуса или повторного `onResume`;
  - `MENU` сохранил конечный статус «Робот в сети» и не откатился назад;
  - после сворачивания на 196 секунд PID остался `31472`, `ConnectorForegroundService` сохранил `isForeground=true`, проверки продолжили давать `executed ok=true`;
  - панель показывала свежий `mobile_presence = home_wifi_local`, `local_host = 192.168.1.159`, возраст 8 секунд;
  - журнал `/home/max/AI_OFFICE/local_only/ai-office/logs/task-20260711-cycle3-post-panel-only-fix/20260711-cycle3-complete-adb.log`, SHA-256 `12d2b3f039a2797acad5e1ed18f85dab4b1cbc885d3f427c761b646061515b09`.
- Отдельный P1 платформы по `robot_ws_probe_state` реализован в `hotfix/edge-hub-probe-state`, коммит `10bcbf1`, draft PR `https://github.com/MaxCorpOrg/GOSHA_PLATFORM/pull/25`.
- На текущей точке ещё не подтверждено полностью:
  - активный OTA-раздел двумя независимыми источниками;
  - чистая аудиодиагностика первой и повторной подсказки без Android-нагрузки;
  - живая радиопроверка переходной сети `Xiaozhi-*`.
- Голосовые жалобы пользователя сейчас не считаются мобильным дефектом:
  - тембр и разнообразие голоса ограничены серверным `TTS`-движком платформы
  - мобильный клиент только доводит пользователя до робота и платформы

## Что делать дальше

1. P0: при остановленном Android-коннекторе подтвердить активный OTA-раздел по загрузочному UART и `ota.label`, затем получить чистые `wifi_prompt_diag` первой и повторной подсказки.
2. P1: после аппаратной диагностики выполнить ещё один контрольный Android-цикл как регрессионную проверку, не используя внешние raw TCP-подключения к `:8080`.
3. P1: отдельно проверить восстановление после временно недоступного робота, не используя внешние raw TCP-проверки порта `:8080`.
4. P1: провести review и слияние платформенного PR `#25` по `robot_ws_probe_state`; не дублировать правку в Android.
5. P1: исправить правило показа OEM-инструкции, чтобы `Позже` или простое открытие настроек не скрывало её навсегда без подтверждения `Нет ограничений`.
6. P1: пройти review опубликованных Android- и firmware-веток, не смешивая их с платформой и iOS.
7. P2: обновить выпускной список проверок, закрепить SHA APK и прошивки и подготовить повторяемый регрессионный сценарий Android + прошивка + платформа.
8. Для iOS:
   - основной отдельный контур теперь `GOSHA_MOBILE_IOS`;
   - использовать либо iPhone/iOS, совместимый с `Xcode 13.2.1`, либо более новый Mac/Xcode;
   - либо, если продолжать эксперимент на этой машине, сначала добыть настоящий `DeviceSupport/DDI` для `26.1 (23B85)` из более нового Xcode;
   - затем открыть готовый Xcode-проект;
   - настроить signing и проверить живой сценарий на iPhone;
   - затем перейти к signing и App Store пакету.
9. Если позже появятся новые локальные `AGENTS.md`, переносить в них тот же блок правил русского технического языка.
